<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.partyfit.model.dao.PartyDao">
	<select id="selectParty" parameterType="SearchCondition"
		resultType="Party">
		select
		party_id, name, introduction, introduction_image, exercise_category
		from party
		<if test="key != null">
			where ${key} like concat('%',#{word},'%')
		</if>
		<if test="orderBy != null">
			order by ${orderBy} ${orderByDir}
		</if>
	</select>
	<insert id="insertParty" parameterType="Party">
		insert into party(name, introduction, introduction_image, exercise_category, party_type) 
		values(#{name}, #{introduction}, #{introductionImage}, #{exerciseCategory}, #{partyType})
	</insert>
</mapper>