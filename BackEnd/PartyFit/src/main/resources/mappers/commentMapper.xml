<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.partyfit.model.dao.CommentDao">
	<select id="selectComment" parameterType="int" resultType="Comment">
		select c.content, c.reg_date, c.mod_date, c.likes, c.depth, c.seq, c.article_id, u.username, u.user_id
		from comment c, user u 
		where c.delete_yn = 'N' and c.user_id = u.user_id and c.article_id = #{articleId}
	</select>
</mapper>